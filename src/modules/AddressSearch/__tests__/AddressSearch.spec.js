import React from 'react';
import { render } from 'test-utils';
import { AddressSearch } from 'modules/AddressSearch';

describe('Test Address Search page', () => {
  test('handles search results correctly', () => {
    expect(true);
  });
});
